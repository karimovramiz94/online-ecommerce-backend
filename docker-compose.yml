version: '3.8'
services:
  db:
    image: postgres:15
    container_name: ecommerce_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 21091994
      POSTGRES_DB: ecommerce_db
    ports:
      - "5433:5432"  # Мы меняем внешний порт на 5433, чтобы не было конфликтов